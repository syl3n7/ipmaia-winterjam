# Development Environment Variables (with database enabled)
NODE_ENV=development
DEV_BYPASS_AUTH=true
PORT=3001

# Database configuration (Docker development with real database)
DB_HOST=localhost
DB_NAME=winterjam_dev
DB_USER=postgres
DB_PASSWORD=postgres
DB_PORT=5432

# Session security (change these in production!)
JWT_SECRET=dev-jwt-secret-change-in-production
SESSION_SECRET=dev-session-secret-change-in-production

# CSRF configuration
# CSRF is enforced in production. For convenience during local development, the server skips CSRF checks for admin endpoints when NODE_ENV != 'production'.
# You can explicitly document your preference here (server currently uses NODE_ENV to determine dev bypass):
# DEV_BYPASS_CSRF=true  # optional: enable to explicitly bypass CSRF checks in development

# Frontend URL for CORS (disabled for development)
FRONTEND_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# PocketID API Configuration
POCKETID_API_URL=https://pocketid.your-domain.com
POCKETID_API_KEY=your-api-key-here

# OIDC Configuration - DEPRECATED
# OIDC_ISSUER_URL=
# OIDC_CLIENT_ID=
# OIDC_CLIENT_SECRET=
# OIDC_REDIRECT_URI= http://localhost:3001//api/auth/oidc/callback
# OIDC_ADMIN_EMAIL=

# AUTO-MIGRATION SETTINGS (Optional)
# These control the automated database migration system
# Defaults are usually fine, but can be customized if needed:
# HEALTH_CHECK_TIMEOUT=5000          # Health check timeout in ms
# MIGRATION_MAX_RETRIES=30           # Max attempts to wait for backend health
# MIGRATION_RETRY_DELAY=10000        # Delay between health check retries in ms

# Public registration & dev flags
PUBLIC_REGISTRATION_ENABLED=true
ALLOW_DEV_AUTOLOGIN=false

# Hashing configuration (Argon2 recommended; install argon2 to enable)
BCRYPT_SALT_ROUNDS=12
ARGON2_TIME_COST=3
ARGON2_MEMORY_COST=65536
ARGON2_PARALLELISM=1


# Backend
# Using DB_HOST/DB_NAME/DB_USER/DB_PASSWORD/DB_PORT — DATABASE_URL removed to avoid redundancy
